[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers
markers =
    unit: 单元测试标记
    integration: 集成测试标记  
    slow: 耗时较长的测试标记
    api: API测试标记